// 1.Funkcija spausdinamas tekstas
function tekstas(x) {
    console.log(x);
}
tekstas ('tekstas');
// 2. Argumentas kiek kartu spausdint
function tekstasKartai(x) {
    kartai = 3;
    console.log(x.repeat(kartai));
}
tekstasKartai('tekstas');
// 4. Dalijasi be liekanos
function beLiekanos(a){
    remainder = a / b;
    if(b = 1 || a = b){
        
    }
}